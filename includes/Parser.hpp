#ifndef PARSER_HPP
#define PARSER_HPP

#include "Libraries.hpp"


//for processing configuration data. 

// Configuration parsing - parse and extract various types of data (strings, numbers, booleans, arrays, maps) from configuration files or strings.
// Data cleaning-  functions to remove spaces, trim strings, and clean up input data.
// Validation -methods to check and validate specific types of configuration lines, such as server configurations.
// templates to handle different data types, for various parsing needs.
//error checking to ensure data integrity during parsing.
// Structured data extraction: It should parse nested structures, such as server configurations with multiple parameters.


#endif